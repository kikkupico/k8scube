import { FaceFrame } from "./FaceFrame";

export function FoundationsFace() {
  return (
    <FaceFrame face="foundations">
      {/* Dynamic foundation items (Namespaces, RBAC) to be added later */}
    </FaceFrame>
  );
}
