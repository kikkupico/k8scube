import { FaceFrame } from "./FaceFrame";

export function StorageFace() {
  return (
    <FaceFrame face="storage">
      {/* Dynamic storage items (PVs, PVCs, ConfigMaps) to be added later */}
    </FaceFrame>
  );
}
